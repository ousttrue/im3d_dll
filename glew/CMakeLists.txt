SET(SUBNAME glew)
FILE(GLOB SRC
        GL/glew.c
    ) 
ADD_LIBRARY(${SUBNAME} SHARED
    ${SRC}
    )
TARGET_INCLUDE_DIRECTORIES(${SUBNAME} PUBLIC
    .
    )
TARGET_COMPILE_DEFINITIONS(${SUBNAME} PRIVATE
    GLEW_BUILD
    )
TARGET_LINK_LIBRARIES(${SUBNAME}
    OPENGL32
    )
